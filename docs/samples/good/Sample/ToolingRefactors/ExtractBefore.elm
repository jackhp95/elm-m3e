module Sample.ToolingRefactors.ExtractBefore exposing (..)

{-| GENERATED by `npm run gen:samples` — do not edit.

This is the Elm shown at `Route.Guide.ToolingRefactors.extractBefore`, compiled for real.
Change the sample there; this file follows.

-}

import Html
import Html.Attributes
import M3e
import M3e.Unsafe
import M3e.Unsafe.Attributes


-- a raw escape inlined in a feature module, for something the library
-- already models: `class` has a typed setter, so this is a needless escape
shown0_ =
    M3e.button [ M3e.Unsafe.Attributes.fromHtmlAttribute (Html.Attributes.class "flex-auto") ] [ M3e.text "Save" ]
