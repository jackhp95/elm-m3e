// Bundled by esbuild into public/m3e.js (a static module linked from
// elm-pages.config.mjs). Registers every @m3e/web custom element so the
// <m3e-*> tags the Ui.* library renders upgrade in the browser.
import "@m3e/web/all";
// The <avt-snackbar> declarative wrapper that Ui.Snackbar.view renders.
import "../js/avt-snackbar.js";
