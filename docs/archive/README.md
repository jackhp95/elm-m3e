# Archived planning docs

> **⚠ Stale — frozen point-in-time snapshots. Do not trust as current state.**

These were working handoff/plan trackers from earlier eras of the project
(largely the `Ui.*` module architecture, since superseded). They describe
component counts, module names, and "remaining work" that no longer match the
codebase, and they were never kept in sync as `main` advanced.

For current truth, use:

- **Architecture:** [`docs/adr/0006-m3e-architecture.md`](../adr/0006-m3e-architecture.md)
  (the `M3e.*` facade + introspectable IR + phantom-typed slots) and the other ADRs.
- **Outstanding work:** the live GitHub issues (e.g. the #52 facade epic, #6 publish,
  and the cross-repo `jackhp95/elm-cem#1` generator retarget).
- **What actually shipped:** `git log`.

Kept only for historical reference.
