# Plan — B1.5c: Converter child-routing fix + cheap attr buckets

Spec: `docs/superpowers/specs/2026-07-07-b1.5c-child-routing.md`
Branch: `docs/b1.5c-child-routing` (created off `main` @ `104a2ff`).
cwd: repo root unless noted. Config lives at REPO ROOT `config/` (not under `docs/`).

## Files & seams (from investigation, verify line numbers by grep — they drift)
- `docs/scripts/examples-gen/lib/oracle.mjs` — CEM+slots loader.
  - CEM read + entry keying by `d.tagName`: ~line 48-49, 67-70.
  - Helper-name derivation: ~157-165. **Default-slot config-key bug:** ~166-167 (`"default"` vs
    `"unnamed"`), short-circuit at ~216-219.
  - `entry.slots[]` shape `{ rawName, helper, kind, required, multi }`: built ~184.
- `docs/scripts/examples-gen/lib/to-elm.mjs` — the converter.
  - `isDroppableAttr`: ~61-65 (used at ~315 top, ~615 mid/bottom).
  - `ARIA_SETTER` map (setter-map path to mirror): ~38-43.
  - Child grouping loop: ~353-394; **default-slot fallback (routing seam):** ~383.
  - default `child`/`children` wrap: ~406-430; `content`-fold: ~243-248, folded ~396-415.
- `docs/scripts/examples-gen/examples-to-elm.mjs` — regen driver + per-surface degradation
  (verifyLayer ~218-244; null-per-surface merge ~263-269).
- Output data: `config/examples.rich.json` (per example `{title, section?, html, top, mid, bottom}`,
  surfaces `string|null`), `config/examples.generated.json`, `config/examples.skipped.txt`.
- Tests: `docs/scripts/examples-gen/lib/to-elm.test.mjs` (3 failing now), `…/extract-matraic-examples.test.mjs`.

## Steps

### Step 0 — Baselines (capture BEFORE any edit)
- `cd docs && node --test scripts/examples-gen/lib/to-elm.test.mjs 2>&1 | tail -5` → record pass/fail
  counts (expect `pass 29, fail 3`).
- Find the regen command: inspect `docs/package.json` scripts for the examples regen (likely
  `build:examples` or similar; the raw driver is `node scripts/examples-gen/examples-to-elm.mjs`
  run from `docs/`, but USE the npm script if one exists so cwd/paths are right).
- Record baseline metrics from the CURRENT committed `config/examples.rich.json`:
  - Total example count; count of examples with non-null `top`; the list of composite examples with
    `top: null` (Tabs, Stepper, SplitButton, Tree, Select, RichTooltip, Step). A one-liner:
    `node -e 'const j=require("./config/examples.rich.json"); ...'` (mind cwd: config is repo-root).
  - Grep `config/examples.skipped.txt` for `m3e-stepper-next` / `m3e-fab-menu-item` / `unknown m3e tag`.
- Save these numbers into the implementation report — every "holds or rises" claim is measured
  against them.

### Step 1 — Fix A: tagName reconciliation in `oracle.mjs`
After the CEM is read and BEFORE entries are keyed by `d.tagName`, walk `cem.modules[].exports`,
and for each `kind === "custom-element-definition"` set the referenced declaration's `tagName` to the
export `name` (resolve `declaration.module` if present, else by name; only when the decl has
`customElement` truthy and the value differs). Keep it a small local helper with a WHY comment
pointing at the same analyzer defect B1.5b fixed. Verify: after this, `buildOracle()` yields entries
for `m3e-stepper-next` and `m3e-fab-menu-item` (quick `node -e` probe).

### Step 2 — Fix B: default-slot config key
Fix `oracle.mjs:166-167` so the default slot (`rawName===""`) reads config from `"unnamed"` (and
still `"default"` for back-compat). Ensure the CEM-slot loop doesn't pre-seed `seenRaw` with an empty
config that blocks the later config-merge (lines ~216-219) — the cleanest fix is to resolve the
default-slot cfg with `slotConfig["unnamed"] ?? slotConfig["default"] ?? {}` at the point of use so
`required/multi/kinds` are present on the CEM-derived slot. Re-run the unit tests — tests 4, 6, 18
must now PASS with no new failures.

### Step 3 — Fix C: child-tag routing by produced kind
- In `oracle.mjs`, derive per container a map from a child's produced KIND to the slot helper that
  accepts it, from the container's named slots (`entry.slots` where `rawName !== ""` and the slot's
  `kind` is a specific child kind). Expose as `entry.childSlotByKind` (or similar). EXCLUDE kinds that
  belong to the container's default `children` union (so Menu-family union children are NOT pulled into
  named slots).
- Need each child TAG's produced kind. The oracle knows each tag's module; the produced kind is that
  module's kind (grep how kinds are currently associated with modules — reuse the existing kind data,
  don't invent). Build a `tag → kind` lookup if not already available.
- In `to-elm.mjs` at ~line 383 (the no-`slot=` default fallback): compute the child's produced kind
  from its tag; if `entry.childSlotByKind[childKind]` exists, push
  `M3e.${mod}.${helper} (${nodeToElm(child, oracle)})` into `slottedExprs` (single Content) instead of
  `defaultExprs`. Else unchanged.
- **Verify the derived routing equals the authoritative table** (memory `docs-composite-routing-table`)
  for Tabs, Stepper, Tree, NavMenuItem, Step, RichTooltip, Select. If any container is ambiguous or
  mismatches, add an explicit per-tag override for JUST that container and note it. Confirm Menu /
  MenuItemGroup / NavMenu / FabMenu(`m3e-menu-item`) emit IDENTICAL `top` to baseline (Step 5 diff).

### Step 4 — Fix D: cheap attr buckets
- Add `m3e-toc-ignore` to `isDroppableAttr` (`to-elm.mjs:61-65`).
- For `hidden`/`autofocus`: confirm they already route via the per-component attr path where a setter
  exists; only add to `isDroppableAttr` if they currently leak as skips AND no component setter exists.
  Do NOT invent a universal setter. Keep the diff minimal.

### Step 5 — Regenerate & verify
- Regenerate examples (the Step 0 command). This recompiles every surface per example (elm make inside
  the driver) and rewrites `config/examples.rich.json` / `examples.generated.json` / `examples.skipped.txt`.
- Re-measure vs. Step 0 baselines:
  - to-elm unit tests: all pass.
  - `top` non-null now includes Tabs/Stepper/SplitButton/Tree/Select (spot-check the actual Elm string
    is a real `M3e.<Composite>.view …`, not a fallback).
  - `m3e-stepper-next`/`m3e-fab-menu-item` gone from `unknown m3e tag` skips.
  - Total count HELD OR ROSE; no example dropped (B1.5a invariant). Any newly-skipped example must be a
    genuine missing-required-slot case — list and justify each (R2).
  - Union-row composites' `top` unchanged (diff `config/examples.rich.json` for Menu/NavMenu/FabMenu).
- `cd docs && npm run build:ci` → exit 0 (`check-nav: OK`, `Success - Adapter script complete`).
- If any `.elm` was hand-edited (shouldn't be), `elm-format --yes` then `--validate`.

### Step 6 — Do NOT commit
Leave the tree dirty for review. (Note: `config/examples.rich.json` etc. ARE tracked in the parent
repo and WILL show in `git status` — unlike `docs/data/reference.json` which is gitignored.)

## Deliverable for review
1. Diffs of `oracle.mjs` and `to-elm.mjs` (and any minimal `config/slots.json` addition under R3).
2. Step 0 baselines vs Step 5 after-numbers, side by side (total count, non-null-`top` count, the
   composite list that flipped to non-null, skip-file delta).
3. The derived `childSlotByKind` map for each composite, checked against the authoritative routing
   table; note any explicit override you had to add.
4. Proof union-row composites are unchanged (diff excerpt).
5. `node --test to-elm.test.mjs` output showing all pass; `extract-matraic-examples.test.mjs` 3/3.
6. `build:ci` tail proving exit 0 + `check-nav: OK` + `Success - Adapter script complete`.
7. A list of every example newly-skipped or newly-degraded (should be none, or only genuine API-gap
   cases), each justified.
