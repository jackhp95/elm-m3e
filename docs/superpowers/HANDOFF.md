# Docs Overhaul — Handoff (2026-07-07)

You are picking up a multi-stream overhaul of the elm-m3e documentation site
(`docs/`, an elm-pages v3 app deployed to Netlify at elm-m3e.netlify.app). Three streams
are **shipped**; five remain. This doc tells you exactly what's done, what's next, where the
code is, and how to run — **autonomously, as an orchestrator**.

> **Your operating mode (the user explicitly asked for this):** act as an *orchestrator*. Keep
> your own context lean. For each unit of work, spawn subagents (investigate → implement →
> review) and hold only the conclusions. You have **full autonomy**: after a stream's work
> passes review and gates, **merge to `main` and push (deploy) yourself** — do not wait for the
> user. Run the **entire remaining queue (B1.5b → D)**. Only surface to the user if you hit a
> genuinely irreversible product decision with no reasonable default (rare — the user's stated
> preference is "embrace breaking changes; uniformity > per-component ergonomics; it just works").

---

## 1. Current state (verified 2026-07-07)

- **`main` HEAD = `523dffc`**, pushed to `origin/main`. Netlify deploys from `main` via
  `pnpm run build:ci`.
- **Shipped & live:**
  - **Stream A** — nav 404s fixed (stale `componentList` slugs remapped, drift guard added),
    live previews use block flow so full-width components fill width.
  - **Stream B1** — examples sourced from matraic's complete `.showcase` markup (not the trimmed
    `.example` `<pre>`); ~192 hollow previews fixed.
  - **Stream B1.5a** — graceful per-surface degradation: an example ships if *any* API layer
    compiles (HTML always does); the renderer shows only the valid surface tabs.
  - **Net result now live:** 329 examples (was 265), **0 hollow previews, 0 empty pages**.
- **Branch `docs/example-sourcing`** is merged into `main` (safe to delete).
- **Working tree:** clean except untracked `m3e-docs/` (a SEPARATE, unrelated project — a Claude
  skill generator; **do not touch or conflate it with the website**).

Verify before starting: `git -C /Users/jack/Documents/code/elm-m3e log --oneline -1` should show
`523dffc`. If `main` has moved, reconcile before assuming the below line refs.

---

## 2. The remaining queue (run in this order)

Each stream = its own cycle: **(design if needed) → spec → plan → subagent-implement → 2-stage
review → merge+push**. Specs go in `docs/superpowers/specs/`, plans in `docs/superpowers/plans/`.
Branch per stream off `main` as `docs/<stream>`.

### B1.5b — Regenerate the Elm library from @m3e/web 2.5.13 (NEXT)

**Why:** the generated Elm library `packages/m3e` (v1.0.0, "generated by elm-cem") is **behind**
the web components the site renders. Proof: `m3e-stepper-next` and `m3e-fab-menu-item` are real
custom elements in installed `@m3e/web@2.5.13` (and in the matraic source), but there is **no
`packages/m3e/src/M3e/StepperNext.elm` / `FabMenuItem.elm`**. See memory
`docs-example-pipeline-constraints` (Cause 2).

**Where:** the library generator is at repo-root **`elm-cem/`** — `elm-cem/codegen/Generate.elm`
and `elm-cem/cem-configs/`. First task for a subagent: **map how the generator is invoked and
where it sources the @m3e/web CEM** (the CEM was NOT found at
`docs/node_modules/@m3e/web/custom-elements.json` — the generator may build/fetch it, or read it
from `cem-configs/` or the matraic clone at `../matraic-m3e`). Then regen `packages/m3e` against
2.5.13.

**Done when:** `StepperNext.elm` / `FabMenuItem.elm` (or the equivalent barrel functions
`M3e.stepperNext` / `M3e.fabMenuItem`) exist; `docs` reference regenerates (`npm run
build:reference` in `docs/`); `build:ci` green. Note: regen can **ripple** widely (many modules +
`docs/data/reference.json` + the barrel). Diff carefully and review the whole generated surface,
not just the two new modules. This unblocks B1.5c routing those two tags.

**Reference:** memory `elm-cem-retarget-design`, `barrel-and-rules-architecture`.

### B1.5c — Converter child-routing fix + cheap attr buckets

**Why:** `docs/scripts/examples-gen/lib/to-elm.mjs` (`elementToElm`, the block commented
`// --- Children: group by slot.` at ~line 344) lumps ALL slot-less children into one
`M3e.<mod>.children [ … ]` (built at ~line 429). That only type-checks when `children`'s phantom
row is a UNION over every child kind. It breaks for composites whose children route to DIFFERENT
helpers → they currently ship HTML/bottom-only (partial toggle). Fixing this restores full 4-tab
parity.

**The routing table is in memory `docs-composite-routing-table`** (exact per-container child-tag →
helper map). Key fixes: Tabs (`m3e-tab-panel` → `M3e.Tabs.panel`, not `children`), Stepper
(`m3e-step`→`step`, `m3e-step-panel`→`panel`; no `children`), SplitButton (slot-only:
`leadingButton`/`trailingButton`), Tree/NavMenuItem/Step/RichTooltip/Option/Select (route
named-slot children to their slot helpers). Union-row components (Menu/NavMenu/FabMenu) already
work — don't change them.

**Cheap buckets:** misc unmapped attrs (`m3e-toc-ignore`, `hidden`, `autofocus`) — add to
`isDroppableAttr` (to-elm.mjs ~line 61) or map to setters. (The `<script>` "Native module support"
filter was already done in B1.5a.)

**Leave alone (genuine API gaps, no path even in current lib):** `m3e-optgroup` inside
`m3e-select`, Menu submenus. Graceful degradation already ships these at HTML/bottom — do NOT
force them; that's deferred library-design work.

**Done when:** composite pages (tabs, stepper, splitbutton, tree, select) regain the strict `M3e`
tab (verify with playwright — the `M3e` label reappears in the tab strip); example count holds or
rises; `build:ci` green. **Depends on B1.5b** for the stepper-next/fab-menu-item bindings.

### B2 — Auto-derived code folding (fully designed already)

**The design is complete in the spec** — see
`docs/superpowers/specs/2026-07-07-example-sourcing-and-folding-design.md`, § B2. Summary: native
`<details>` (no CodeMirror). Two fold types: **indentation folds** (chevron, computed in the
renderer from code indentation → applies to all surfaces with zero generator work) and
**repeated-sibling folds** (ellipsis `…`, generator-marked). Default-collapsed state is
**matraic-guided** via the `brevitySource` field (already captured on every example by B1 — check
`config/examples.rich.json`), with a size heuristic fallback.

**Where:** `docs/src/Doc.elm` — `code_` (line ~84, syntax-highlight seam; add fold
parsing/rendering here), `rawPreview` (line ~53). Renderer glue in `Name_.elm`. Consult
`modern-web-guidance` skill → guide `search-hidden-content` (native `<details>` stays find-in-page
searchable). Verify folds + Ctrl-F reveal with playwright.

### C — Tab/panel sliding (needs a design cycle)

**Product intent (from the user's original brief):** Material tabs should *slide* — clicking a
tab slides the prior panel out and the new one in — instead of DOM-swapping content. This applies
to the **API-surface toggle** on component pages (the `M3e / M3e.Cem / … / HTML` tabs), which
today just swaps `codeFor`'s output in place. Code: `Name_.elm` `layerTabs` (line ~468) +
`codeFor` (~490).

**Run a design cycle** (brainstorm → spec → plan). Approach hint: a horizontal track with all
panels, `translateX` by selected index, or the View Transitions API — consult `modern-web-guidance`
(view transitions / scroll-driven). Keep it lightweight; no heavy deps. Make the product calls
yourself.

### D — Docs app shell (needs a design cycle)

**Product intent (from the user's original brief), all in `docs/app/Shared.elm`:**
1. Emulate **matraic's mobile app-bar pattern** at the top (reference the matraic clone's
   `../matraic-m3e/docs/` app-bar/drawer CSS + markup for the pattern). `viewAppBar` ~line 395.
2. Emulate **matraic's settings-drawer pattern** for our settings drawer. `settingsPanel` ~line
   508.
3. Use the **GitHub logo** instead of the generic code glyph for the GitHub link. `githubLink`
   (line ~442) currently uses `Icon.name "code"` — swap for a real GitHub mark (inline SVG via a
   Native/raw node, since Material Symbols has no brand logo).
4. **Remove the light/dark quick-toggle from the app bar** — `schemeQuickToggle` (line ~458, wired
   at ~407) — because that control already lives inside the settings drawer.

**Run a design cycle.** Reference memory `docs-mobile-polish-and-surfaces` (KEY LESSON: Elm can't
set CSS vars → use Tailwind `[--var:val]` classes).

---

## 3. How to work — conventions & the non-negotiable verification bar

**Orchestration:** per stream, YOU write the spec + plan (these are fast, keep them in context),
then dispatch a **general-purpose implementer subagent** with the plan path + scene-setting +
risk flags. Then dispatch **two review subagents** (spec-compliance, then code-quality) over the
implementation diff. Fix loops go back to a subagent. This pattern is proven this session — copy
the subagent prompts from the plans' sibling commits / prior handoff turns.

**Verification bar (every stream, before merge):**
- `cd docs && npm run build:ci` → **exit 0** (this is exactly what Netlify runs; guard prints
  `check-nav: OK`, ends `Success - Adapter script complete`).
- `docs/node_modules/.bin/elm-format --validate app src` → `[]` after any `.elm` edit (run
  `elm-format --yes <file>` first; see memory `elm-format-after-edits`).
- **Playwright for EVERY visual claim** (the user insisted). Serve dist and assert on the real
  DOM. Recipe:
  - Build: `cd docs && npm run build`. Serve: `node scripts/serve-dist.mjs dist 1239 &`
    (kill stale first: `pkill -f serve-dist.mjs`).
  - Import chromium (bare `playwright` is NOT installed): 
    `import pkg from "/Users/jack/Documents/code/elm-m3e/docs/node_modules/.pnpm/@playwright+test@1.61.1/node_modules/@playwright/test/index.js"; const { chromium } = pkg;`
  - Custom elements need ~1500ms after `networkidle` to upgrade before you measure.
  - Put scratch scripts + screenshots under the session scratchpad, not the repo.
- Only after all of the above: `git checkout main && git merge --no-ff docs/<stream> -m "…"`
  then `git push origin main`. Merge commit body should summarize + note "reviews passed".

**Git conventions:** branch per stream off `main`; never force-push / reset --hard / clean.
Commit trailer (every commit):
```
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Ap4D1B7LnEde7MVfd5uuCy
```

---

## 4. Gotchas & non-obvious facts (read before touching code)

- **`config/` is at the REPO ROOT**, not under `docs/` (holds `examples.matraic.json`,
  `examples.rich.json`, `examples.skipped.txt`, `categories.json`). The npm scripts resolve it;
  your manual `node -e` one-liners must use the right relative path per cwd.
- **The all-or-nothing layer invariant is GONE** (removed in B1.5a). Examples now degrade
  per-surface. Don't reintroduce a "drop unless all layers compile" gate.
- **`brevitySource`** is on every example record (added by B1) — the trimmed matraic snippet,
  the fold oracle B2 needs. Don't strip it.
- **3 pre-existing test failures** in `docs/scripts/examples-gen/lib/to-elm.test.mjs` (converter
  slot-record shape). They predate this work — verify they fail identically at your base commit
  before blaming your change. `extract-matraic-examples.test.mjs` passes 3/3.
- **Sources:** matraic clone (example source) at `../matraic-m3e` @ commit `c9c781a`; installed
  web components `@m3e/web@2.5.13`. The `MATRAIC_DIR` env overrides the clone path.
- **linkedom `innerHTML` preserves whitespace** fine (verified) — no pretty-printer needed for
  `.showcase` extraction.
- The **drift guard** (`docs/scripts/check-nav.mjs`) hard-fails on a drawer slug with no page and
  warns on an example-bearing page missing from nav. When you recover more examples, new pages may
  become example-bearing — add them to `componentList` (`docs/app/Shared.elm`, ~line 809) to clear
  the warning (that's how `navmenu` got added).

---

## 5. Reference index

- **Spec (B1/B1.5/B2):** `docs/superpowers/specs/2026-07-07-example-sourcing-and-folding-design.md`
- **Spec (Stream A):** `docs/superpowers/specs/2026-07-07-docs-quick-fixes-design.md`
- **Plans:** `docs/superpowers/plans/2026-07-07-docs-quick-fixes.md`,
  `…-example-sourcing-b1.md`, `…-graceful-degradation-b1.5a.md`
- **Memory (auto-loaded each session, but read the files for detail):**
  - `docs-example-pipeline-constraints` — the 3 root causes (invariant, version skew, routing)
  - `docs-composite-routing-table` — the per-container child→helper map for B1.5c
  - `elm-cem-retarget-design`, `barrel-and-rules-architecture` — library/generator architecture
  - `docs-mobile-polish-and-surfaces` — Stream D reference (CSS-var lesson)
  - `feedback-prerelease-perfection` — the user's autonomy/uniformity preferences
  - `elm-format-after-edits` — run elm-format after every `.elm` edit
- **Key code seams (symbol names — line numbers may drift, grep to confirm):**
  - Converter: `docs/scripts/examples-gen/lib/to-elm.mjs` `elementToElm` (child routing ~344-430)
  - Generator/degradation: `docs/scripts/examples-gen/examples-to-elm.mjs`
  - Renderer: `docs/app/Route/Components/Name_.elm` — `UsageExample`, `layersFor`,
    `defaultLayerFor`, `layerTabs`, `codeFor`
  - Code/preview display: `docs/src/Doc.elm` — `code_`, `rawPreview`, `showcase`
  - App shell (Stream D): `docs/app/Shared.elm` — `viewAppBar`, `githubLink`, `schemeQuickToggle`,
    `settingsPanel`, `componentList`
  - Library generator (B1.5b): `elm-cem/codegen/Generate.elm`, `elm-cem/cem-configs/`
