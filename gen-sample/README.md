# Generated-output sample — elm-cem retarget proof-of-pipeline (2026-06-30)

These files are **generated by the retargeted elm-cem** (branch `retarget` on
jackhp95/elm-cem), shown so you can see the shape. They are a sample, not wired
into the build. Full status: ../REMAINING.md. Frictions: ../FRICTIONS.md.

- `M3e/Button.elm` — TOP layer: `view : List (Option msg) -> List (Element child msg)
  -> Element { s | button : Supported } msg`, built from the `M3e.Node` IR, with
  config-endomorphism option setters. Imports the hand core (c1 deferred).
- `M3e/Cem/Button.elm` — MIDDLE layer (Value-attrs + Html children, returns Html).
- `M3e/Cem/Html/Button.elm` — BOTTOM layer (plain elm/html element constructor).
- `Sl/Cem/Html/Alert.elm` — the SAME generator on Shoelace, config-free (genericity).
- `tests/GenButtonTest.elm` — the 4 IR assertions that PASS against the generated
  top-layer Button (tag, disabled property, href attr, R1 boolean-reset).

Not yet emitted in the top layer: enum→Value, typed slots, events, required
content (see REMAINING.md).
