# Generated snapshot — the real generator output (2026-06-30)

GENERATED by the retargeted elm-cem (branch `main` on jackhp95/elm-cem), to the
locked design in [../docs/THREE_LAYER_PATTERN.md](../docs/THREE_LAYER_PATTERN.md).
**The whole library (125 components) compiles.**

- `M3e/Cem/Html/{Button,Icon}.elm` — BOTTOM: partial-applied elm/html.
- `M3e/Cem/{Button,Icon}.elm` — MIDDLE: capability-typed Attr setters reusing the
  bottom; `Value`-typed enum setters; eager component.
- `M3e/{Button,Icon}.elm` — TOP: lazy IR; aliased setters; kind-row result.
- `M3e/Value.elm` — GENERATED Value vocabulary (~140 manifest-wide tokens).
- `M3e/Value/Core.elm`, `M3e/Node.elm`, `M3e/Element.elm`, `M3e/Cem/Attr.elm` —
  hand-written generic core (generation deferred — F7).

Verified against this generated output:
- `Positive.elm` — `variant Value.filled`, `disabled True`, icon child → **compiles**.
- `Neg1_wrongValue.elm` — `variant Value.small` → **rejected** (small ∉ closed Value row).
- `Neg2_foreignAttr.elm` — foreign-capability attr → **rejected**.

Pending (needs config, Decl): typed-slot CHILDREN (the 3rd phantom row); events; required content.
